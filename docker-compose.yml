services:
  n8n:
    image: docker.n8n.io/n8nio/n8n
    ports:
      - '5678:5678'
    volumes:
      - ~/.n8n:/home/node/.n8n
  api:
    build:
      context: ./api
    ports:
      - '8000:8000'
    volumes:
      - ./api:/api
    env_file:
      - .env
